# Examples where the input contains only source/config files like JSON data files
# No build artifacts, dependencies, or temp files present
# Following rule 6: output empty since only source files are in the change list

# (empty - no files need to be ignored)